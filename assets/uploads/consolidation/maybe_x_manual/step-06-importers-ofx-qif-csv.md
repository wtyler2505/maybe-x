# STEP 6 — Importers (OFX/QIF/CSV)


Node microservice parses files with `ofx`, `ofx-js`, `qif` and `csv-parse`. CSV mapping via bank2ynab-style YAML profiles.
Rails uploads → POST to service → normalized JSON → upsert.


---
[Back to main manual](./maybe-x_consolidation_manual.md)
