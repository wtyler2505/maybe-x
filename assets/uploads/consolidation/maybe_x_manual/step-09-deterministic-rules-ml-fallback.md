# STEP 9 — Deterministic rules + ML fallback


Rules in JSON (contains, equals, regex, ranges). Validate with JSON Schema.
Fallback ML: RapidFuzz aliasing, SetFit few-shot classifier or LightGBM tabular model exposed via Python service.


---
[Back to main manual](./maybe-x_consolidation_manual.md)
